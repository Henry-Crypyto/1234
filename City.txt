$list = array(); // 存储查询结果的数组
switch ($action) {
    case 'city': // 查询城市数据
        $sql = "SELECT * FROM city WHERE 1"; // 从 city 表中选择所有记录
        $result = mysqli_query($link, $sql) or die("查询失败");

        while ($row = mysqli_fetch_assoc($result)) { // 遍历结果集并存入$list数组
            $list[] = $row; // 将每一行记录存入数组
        }
        break;
    // 可能还有其他的 case 语句来处理不同的 $action
}